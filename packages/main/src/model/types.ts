export interface TodoProps {
    id: number;
    completed: boolean;
    title: string;
}
export interface UserProps {
    username: string;
    firstName: string;
    lastName: string;
    role: string;
}
export interface SignInProps {
    username: string;
    password: string;
}
